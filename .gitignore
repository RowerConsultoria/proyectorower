# Sistema
.DS_Store
Thumbs.db

# Temporales de edición
*.bak
*.tmp
backup_*.html
~$*

# Node (si algún módulo llega a usar tooling)
node_modules/

# Insumos de trabajo (material confidencial del cliente: entrevistas, docs de
# Lark, Estructura Patrón, mapa v18). Se leen en local; nunca al repositorio.
Insumos/

# Workspace local del editor
scripts/Rower.code-workspace
*.code-workspace

# Python (caché de los guiones de scripts/)
__pycache__/
*.pyc

# Credenciales — nunca al repositorio
.env
.env.*
*.secret
scripts/.qa_creds
_ses_temp.json

# Supabase CLI (caché local)
supabase/.temp/

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
!.env.example
